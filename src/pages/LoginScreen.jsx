import React from "react";
import LogIn from "../component/loginComp/LogIn";

const LoginScreen = () => {
  return (
    // i will update data inside the component page
    <LogIn />
  );
};

export default LoginScreen;
